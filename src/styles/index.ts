import { createStitches } from '@stitches/react'

export const {
  config,
  styled,
  globalCss,
  css,
  keyframes,
  getCssText,
  createTheme,
  theme
} = createStitches({
  theme: {
    colors: {
      backgroud: '#000027'
    }
  }
})
